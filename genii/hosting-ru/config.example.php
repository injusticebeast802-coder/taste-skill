<?php
/* =========================================================
   Настройки приёма заявок.

   ЧТО СДЕЛАТЬ: переименуйте этот файл в config.php и впишите
   свои значения вместо многоточий. Файл config.php лежит рядом
   с сайтом, но наружу не отдаётся — доступ к нему закрыт
   в .htaccess. В браузере его содержимое увидеть нельзя.

   Значения — те же самые, что были в настройках Vercel.
   Их можно посмотреть там же: Settings -> Environment Variables.
   ========================================================= */

/* Обязательные: без них заявки приходить не будут. */
define('TG_TOKEN',   '...');   // токен бота, вида 1234567890:AAG...
define('TG_CHAT_ID', '...');   // номер чата, куда бот пишет

/* Необязательные: дублирование заявки на почту.
   Если оставить пустыми, сайт работает как обычно, только
   через Телеграм. */
define('RESEND_API_KEY', '');  // ключ Resend, вида re_...
define('MAIL_TO',        '');  // та же почта, что и на первом сайте
define('MAIL_FROM',      '');  // ГенИИ <zayavki@genii-ai.ru>

/* Домены, с которых разрешено отправлять форму.
   Чужой сайт не сможет слать заявки от вашего имени. */
$ALLOWED_HOSTS = array(
  'genii-ai.ru',
  'www.genii-ai.ru',

  /* Пока домен не переключён, сайт проверяется по временному адресу
     хостинга. Чтобы форма работала и там, впишите этот адрес сюда —
     без http:// и без косой черты в конце. Например:
     'u3604910.isp.regruhosting.ru',
     После переезда строку можно убрать. */
);
